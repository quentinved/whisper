# whisper-secrets platform binary

This is a platform-specific binary package for [`whisper-secrets`](https://www.npmjs.com/package/whisper-secrets). **Don't install it directly** — install the main package and npm will pull in the right binary for your platform:

```bash
npm install -g whisper-secrets
```

Source: https://github.com/quentinved/Whisper
